# LogCL Graph Module

This directory contains the temporal graph model used by WISE to produce dense
entity scores for each query. The WISE root README shows the recommended
ICEWS14 training and export commands.

Install a DGL build that matches your CUDA version before running this module.
Additional graph-side dependency notes are listed in `requirement.txt`.
